# Docs Index

Core docs that are kept and maintained:

- `ARCHITECTURE.md`: system architecture and boundaries
- `DATABASE.md`: schema and initialization rules
- `LOW_COST_DISTRIBUTED.md`: deployment model for low-cost distributed mode
- `TROUBLESHOOTING.md`: common failures and checks
- `DESIGN_SYSTEM.md`: 前端战术遥测(Tactical Telemetry)设计系统 — 改前端必读

Cleanup policy:

- Keep only docs that match current code behavior
- Remove legacy plans and repeated module-level notes
- Keep `backend/sql/init.sql` as the only source of schema truth
