# 后端服务器配置
PORT=3000
NODE_ENV=production

# 前端地址（CORS 配置）
# 本地开发使用 5173，Docker 部署使用 localhost
FRONTEND_URL=http://localhost:5173

# 数据库路径（可选，默认为 ./data/database.db）
# DATABASE_PATH=./data/database.db

# 日志级别（可选，默认 info）
# LOG_LEVEL=info

# FCM 配置
# ⚠️ 注意：FCM 凭证通过 Web 界面自动配置，存储在数据库中
# 无需在此文件手动配置 FCM 相关信息
